# ============================================================================
# Streamlit Cloud-safe dependencies
# Target: Python 3.11/3.14 without PaddlePaddle.
# ============================================================================

# Core app
streamlit>=1.35,<2
opencv-python-headless>=4.10
Pillow>=10.4
numpy>=2.1

# OCR
pytesseract>=0.3.13
# PaddleOCR/PaddlePaddle are intentionally excluded here.
# Streamlit Cloud currently provisions Python versions where Paddle wheels are
# not consistently available. Keep Paddle as a local-only optional install.
#
# Local optional install:
#   pip install paddlepaddle paddleocr

# PDF and Word generation
PyMuPDF>=1.26.3
python-docx>=1.1.2

# Optional fallback OCR, local only:
# easyocr>=1.7.1
# torch>=2.3
# torchvision>=0.18
